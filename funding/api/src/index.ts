// @behuman/funding-api — backend del Funding ZK (CAPA 3).
// Donar/opinar gateado por personhood (membership, no is_verified). Cero PII.
// Servidor HTTP en ./server.ts.
export { app } from "./server.js";
